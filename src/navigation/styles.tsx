import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        alignItems: 'center',
    },
    headerLeftIcon: {
        paddingLeft: 20
    },
    headerRightIcon: {
        paddingRight: 10
    },
});

export default styles;